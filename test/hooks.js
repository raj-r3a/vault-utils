// const sinon = require('sinon');
// const logger = require('winston-logstash-transporter')(__filename);

// // not using the before Each hook to avoid globally initialized stub getting lost
// exports.mochaHooks = {
//   // ran before each test, restoring sinon's sandbox to default state
// //   beforeEach(done) {
// //     sinon.restore();
// //     logger.debug('restored sinon sandbox before each test');
// //     done();
// //   },
// };
